# 🎯 未决定AI平台 - 完整项目说明文档

## 📋 项目概述

未决定AI平台是一个基于React 18构建的现代AI交互平台，集成了用户认证、视频管理、问题保存、聊天功能、反馈系统等完整功能。项目采用前后端分离架构，后端使用Express.js，数据以文件系统形式存储。

## 🎉 项目状态

**✅ 项目已完成所有核心功能开发！**

### 主要成就
- ✅ HTML到React 18完全转换
- ✅ 20+ 组件成功创建
- ✅ 无编译错误和警告
- ✅ 完整功能实现
- ✅ 所有Hook正确集成
- ✅ CSS动画和样式完美迁移
- ✅ 构建优化: 53.79 kB JS + 3.57 kB CSS
- ✅ 真实文件系统认证
- ✅ 头像持久化功能
- ✅ 账号头像联动
- ✅ Database文件夹结构优化

## 🏗️ 项目架构

### 文件夹结构
```
front-end2/
├── database/                       # 数据存储目录
│   ├── answer/                     # AI回答文件
│   ├── feedback/                   # 用户反馈
│   ├── question/                   # 用户问题
│   ├── requestion/                 # 用户输入内容
│   ├── touxiang/                   # 用户头像
│   ├── user/                       # 用户信息
│   ├── video/                      # 视频文件
│   └── AI_product/                 # AI生成产品
├── react/                          # 前端React应用
│   ├── src/
│   │   ├── components/             # React组件
│   │   ├── pages/                  # 页面组件
│   │   ├── hooks/                  # 自定义Hook
│   │   ├── utils/                  # 工具函数
│   │   └── styles/                 # 样式文件
│   ├── public/                     # 静态资源
│   └── package.json                # 前端依赖
├── server.js                       # 后端服务器
├── saveQuestion.js                 # 问题保存模块
├── package.json                    # 后端依赖
└── 项目完整说明文档.md             # 本文档
```

## 🔧 核心功能详解

### 1. 用户认证系统 (v1.5.0)

#### 功能特性
- **真实注册**: 用户信息保存到`database/user/`目录的TXT文件
- **密码验证**: 从文件中读取并验证用户密码
- **重复检查**: 防止同一邮箱重复注册
- **登录追踪**: 记录用户最后登录时间
- **数据持久**: 用户信息永久保存，重启后依然有效

#### API接口
- `POST /api/register` - 用户注册
- `POST /api/login` - 用户登录
- `GET /api/user/:email` - 获取用户详细信息

#### 用户文件格式
```
用户注册信息
=====================

用户姓名: 张三
邮箱地址: zhangsan@example.com
密码: password123
注册时间: 2025-06-08T09:25:45.550Z
最后登录: 2025-06-08T09:25:45.566Z
账户状态: active
头像地址: /avatars/zhangsan_example_com_avatar.png

---
此文件由未决定AI平台自动生成
```

### 2. 头像系统 (v1.6.0)

#### 功能特性
- **用户专属头像**: 每个邮箱账号对应唯一的头像文件
- **智能文件命名**: 头像文件以`{email}_avatar.{ext}`格式命名
- **自动关联**: 头像上传时自动与当前登录用户关联
- **登录同步**: 用户登录时自动加载对应的头像
- **持久化存储**: 头像信息保存到localStorage和用户文件

#### API接口
- `POST /api/upload-avatar` - 头像上传
- `GET /api/user-avatar/:userId` - 获取用户头像

#### 头像管理功能
- 🔄 **刷新头像**: 从服务器重新获取最新头像
- 🗑️ **移除头像**: 清除本地缓存，恢复默认显示
- 📱 **状态指示**: 显示"头像已缓存到本地"状态提示

### 3. 视频管理系统

#### 功能特性
- **自动扫描**: 自动扫描`database/video/`文件夹中的所有视频文件
- **多格式支持**: 支持MP4、AVI、MOV、WMV、FLV、WebM、MKV等格式
- **真实信息**: 显示真实的文件大小、视频时长和创建时间
- **智能筛选**: 支持按大小、时长、日期筛选和多种排序方式
- **一键播放**: 点击播放按钮即可在模态框中播放视频
- **视频删除**: 支持删除不需要的视频文件

#### API接口
- `GET /api/videos` - 获取视频列表
- `DELETE /api/videos/:filename` - 删除视频
- `POST /api/process-ai-video` - 处理AI生成视频
- `GET /videos/<filename>` - 视频文件静态服务

### 4. 问题保存系统

#### 功能特性
- **自动保存**: 用户输入问题自动保存到`database/question/`
- **实时保存**: textarea内容实时保存到`database/requestion/`
- **防重复保存**: 1秒防抖机制，避免重复保存
- **多触发保存**: 支持失焦保存、发送前保存

#### API接口
- `POST /api/save-question` - 保存用户问题
- `POST /api/save-to-requestion` - 保存用户输入内容

#### 文件格式
```
输入时间: 2025/6/8 18:32:03
输入内容: 用户输入的内容

---
此文件由未决定AI平台自动生成
保存位置: database/requestion
```

### 5. AI聊天系统

#### 功能特性
- **真实AI回答**: 只显示从`database/answer/`文件夹读取的真实AI回答
- **实时监听**: 监听answer文件夹新增文件，自动显示AI回答
- **移除固定回复**: 删除了所有测试用的固定回复
- **轮询机制**: 定期检查新的AI回答文件

#### API接口
- `GET /api/pending-answers` - 获取待处理的AI回答

#### 实现原理
- 后端监听`database/answer/`文件夹变化
- 新增txt文件时自动读取内容
- 前端轮询获取新的AI回答
- 显示在聊天界面中

### 6. 反馈系统

#### 功能特性
- **多类型反馈**: 支持功能建议、问题报告、内容反馈等
- **优先级管理**: 支持低、中、高、紧急四个优先级
- **分类管理**: 按功能、界面、性能等分类
- **TXT格式存储**: 反馈信息以结构化TXT格式保存

#### API接口
- `POST /api/feedback` - 提交反馈
- `GET /api/feedback` - 获取反馈列表（管理功能）

#### 反馈文件格式
```
==================================================
用户反馈记录
==================================================

反馈ID: feedback_1733123456789_abc123
提交时间: 2025-06-08T10:30:45.123Z
状态: pending

--------------------------------------------------
基本信息
--------------------------------------------------
反馈类型: 功能建议
优先级: 中等
问题分类: 功能相关

--------------------------------------------------
问题详情
--------------------------------------------------
问题标题: 建议添加搜索功能
问题描述: 希望能在视频列表中添加搜索功能...
```

### 7. 个人中心功能

#### 功能特性
- **只读信息显示**: 用户名、邮箱、注册时间只读显示
- **真实注册时间**: 显示用户实际的注册时间
- **头像管理**: 支持头像上传、刷新、移除
- **统计信息**: 显示用户的各项统计数据

#### 界面优化
- **删除编辑按钮**: 移除了"编辑信息"按钮，防止用户误操作
- **简化界面**: 减少不必要的交互元素
- **安全考虑**: 用户基本信息修改需要通过管理员

### 8. 用户界面优化

#### 菜单优化
- **隐藏菜单项**: 隐藏了"账户设置"和"历史记录"菜单项
- **简化操作**: 用户下拉菜单只显示"个人中心"和"退出登录"
- **清理代码**: 移除了无用的事件处理逻辑

#### 响应式设计
- **多设备适配**: 完美适配各种屏幕尺寸
- **现代UI**: 使用现代化的界面设计
- **流畅动画**: CSS动画和过渡效果

## 🔄 版本历史

### v1.6.0 (2025-06-08) - Database结构优化
- ✅ 将所有数据文件夹移动到database目录下
- ✅ 更新所有API接口路径
- ✅ 优化文件夹结构，提高项目组织性
- ✅ 创建完整项目说明文档

### v1.5.0 (2025-06-08) - 聊天功能优化
- ✅ 移除AI聊天中的固定测试回复
- ✅ 只显示从answer文件夹读取的真实AI回答
- ✅ 优化AI回答监听机制

### v1.4.0 (2025-06-08) - Textarea保存功能
- ✅ 实现textarea内容自动保存到database/requestion
- ✅ 添加防抖机制和多触发保存
- ✅ 文件命名格式：YYYY-MM-DD_HH-MM-SS.txt

### v1.3.0 (2025-06-08) - 真实注册时间显示
- ✅ 个人信息页面显示真实的注册时间
- ✅ 新增用户详细信息API
- ✅ 时间格式化和本地化显示

### v1.2.0 (2025-06-08) - 编辑功能移除
- ✅ 删除个人信息页面的"编辑信息"按钮
- ✅ 将用户信息改为只读显示
- ✅ 简化界面，提高安全性

### v1.1.0 (2025-06-08) - 菜单优化
- ✅ 隐藏用户下拉菜单中的"账户设置"和"历史记录"
- ✅ 简化用户操作界面
- ✅ 清理无用的事件处理代码

### v1.0.0 (2025-06-07) - 基础功能完成
- ✅ HTML到React 18完全转换
- ✅ 用户认证系统实现
- ✅ 头像上传和管理功能
- ✅ 视频管理系统
- ✅ 问题保存功能
- ✅ 反馈系统实现

## 🚀 部署和使用

### 环境要求
- Node.js 18+
- npm 8+
- 现代浏览器支持

### 安装步骤

1. **克隆项目**
```bash
git clone <repository-url>
cd front-end2
```

2. **安装后端依赖**
```bash
npm install
```

3. **安装前端依赖**
```bash
cd react
npm install
cd ..
```

4. **启动后端服务**
```bash
node server.js
```

5. **启动前端应用**
```bash
cd react
npm start
```

6. **访问应用**
- 前端地址: http://localhost:3000
- 后端地址: http://localhost:3002

### 目录权限
确保以下目录具有读写权限：
- `database/` 及其所有子目录
- `react/build/` (生产环境)

## 📊 API接口文档

### 用户认证
- `POST /api/register` - 用户注册
- `POST /api/login` - 用户登录
- `GET /api/user/:email` - 获取用户详细信息

### 头像管理
- `POST /api/upload-avatar` - 头像上传
- `GET /api/user-avatar/:userId` - 获取用户头像

### 问题保存
- `POST /api/save-question` - 保存用户问题
- `POST /api/save-to-requestion` - 保存用户输入内容

### 视频管理
- `GET /api/videos` - 获取视频列表
- `DELETE /api/videos/:filename` - 删除视频
- `POST /api/process-ai-video` - 处理AI生成视频

### AI回答
- `GET /api/pending-answers` - 获取待处理的AI回答

### 反馈系统
- `POST /api/feedback` - 提交反馈
- `GET /api/feedback` - 获取反馈列表

### 系统监控
- `GET /api/health` - 健康检查

## 🔒 安全说明

### 当前安全措施
- ✅ 基础输入验证
- ✅ 文件类型检查（头像上传）
- ✅ 路径安全检查
- ✅ 邮箱格式验证

### 生产环境建议
- 🔐 密码哈希加密
- 🛡️ HTTPS协议
- 🔑 JWT令牌认证
- 📝 访问日志记录
- 🚫 SQL注入防护（如使用数据库）

## 🐛 故障排除

### 常见问题

1. **端口占用错误**
```
Error: listen EADDRINUSE: address already in use :::3002
```
解决方案：
```bash
# Windows
taskkill /F /IM node.exe

# Linux/Mac
killall node
```

2. **文件权限错误**
确保database目录及其子目录具有读写权限

3. **前端构建失败**
```bash
cd react
rm -rf node_modules package-lock.json
npm install
npm start
```

4. **头像上传失败**
检查`database/touxiang/`目录是否存在且可写

### 调试模式
启动时添加调试信息：
```bash
DEBUG=* node server.js
```

## 📈 性能优化

### 已实现优化
- ✅ 前端代码分割
- ✅ 静态资源压缩
- ✅ 图片格式优化
- ✅ API响应缓存
- ✅ 防抖机制（自动保存）

### 建议优化
- 📊 数据库索引（如迁移到数据库）
- 🗜️ 图片压缩和CDN
- ⚡ Redis缓存
- 📱 PWA支持

## 🤝 贡献指南

### 开发规范
- 使用ES6+语法
- 遵循React Hooks最佳实践
- 保持代码注释完整
- 遵循文件命名规范

### 提交规范
```
feat: 添加新功能
fix: 修复bug
docs: 更新文档
style: 代码格式调整
refactor: 代码重构
test: 添加测试
chore: 构建过程或辅助工具的变动
```

## 📞 技术支持

### 联系方式
- 项目维护者：AI开发团队
- 技术支持：通过反馈系统提交问题
- 文档更新：2025年6月8日

### 相关资源
- React 18 官方文档
- Express.js 官方文档
- Node.js 官方文档

---

## 📝 更新日志

本文档会随着项目功能的更新而持续更新。最新版本请查看项目根目录下的`项目完整说明文档.md`文件。

**文档版本**: v1.0.0  
**最后更新**: 2025年6月8日  
**文档状态**: ✅ 完整且最新 