# 智映教匠AI平台技术说明书

## 项目基本信息

**项目名称**：智映教匠AI平台  
**技术架构**：React + Flask + MySQL  
**开发团队**：广州大学网络空间安全学院  
**项目状态**：主体功能已完成  

---

## 一、技术架构

### 1.1 总体架构

智映教匠AI平台采用前后端分离的现代化架构，包含主平台和数据库管理系统两个独立项目：

```
智映教匠AI平台
├── 主平台系统
│   ├── React前端 (端口3000)
│   ├── Flask后端 (端口3003)
│   ├── MySQL数据库
│   └── 文件存储系统
└── 数据库管理系统
    ├── React管理界面 (端口5173)
    ├── Flask API (端口8081)
    └── 数据可视化模块
```

### 1.2 技术栈

**前端技术**：
- React 18 - 现代化前端框架
- Material-UI - 组件库
- Zustand - 状态管理
- React Router - 路由管理
- Axios - HTTP客户端

**后端技术**：
- Python 3.8+ 
- Flask 3.0 - Web框架
- SQLAlchemy - ORM映射
- Flask-JWT-Extended - JWT认证
- Flask-CORS - 跨域支持

**数据存储**：
- MySQL 8.0 - 关系型数据库
- 文件系统 - 媒体文件存储
- JSON字段 - 复杂数据结构

---

## 二、项目文件结构

### 2.1 主平台结构

```
front-end2/
├── app/                           # Flask后端应用
│   ├── __init__.py               # 应用工厂
│   ├── app_config.py             # 配置管理
│   ├── database.py               # 数据库连接
│   ├── models/                   # 数据模型
│   │   ├── user.py              # 用户模型
│   │   ├── video.py             # 视频模型
│   │   ├── question.py          # 问题模型
│   │   ├── answer.py            # 回答模型
│   │   └── feedback.py          # 反馈模型
│   ├── routes/                   # API路由
│   │   ├── auth.py              # 认证API
│   │   ├── video.py             # 视频API
│   │   ├── question.py          # 问题API
│   │   ├── feedback.py          # 反馈API
│   │   └── ai.py                # AI服务API
│   ├── utils/                    # 工具模块
│   └── scripts/                  # 管理脚本
├── react/                        # React前端应用
│   ├── src/
│   │   ├── components/          # React组件
│   │   │   ├── auth/           # 认证组件
│   │   │   ├── chat/           # 问答组件
│   │   │   ├── common/         # 通用组件
│   │   │   ├── search/         # 搜索组件
│   │   │   └── video/          # 视频组件
│   │   ├── pages/              # 页面组件
│   │   ├── hooks/              # 自定义Hooks
│   │   ├── utils/              # 工具函数
│   │   └── App.jsx             # 主应用
│   └── package.json            # 依赖配置
├── database/                    # 数据存储
│   ├── video/                  # 视频文件 (1.8GB)
│   └── touxiang/               # 用户头像
├── requirements.txt            # Python依赖
├── run.py                      # 应用启动
├── start.sh                    # 启动脚本
└── README.md                   # 项目说明
```

### 2.2 数据库管理系统结构

```
db-admin-web/
├── backend/                     # Flask后端
│   ├── app/
│   │   ├── __init__.py         # Flask应用
│   │   └── routes.py           # API路由
│   ├── requirements.txt        # Python依赖
│   └── run.py                  # 启动入口
├── frontend/                   # React前端
│   ├── src/
│   │   ├── components/         # 组件库
│   │   ├── pages/             # 页面组件
│   │   ├── utils/             # 工具函数
│   │   └── App.jsx            # 主应用
│   ├── package.json           # 前端依赖
│   └── vite.config.js         # Vite配置
├── start.sh                   # 一键启动
└── README.md                  # 项目文档
```

---

## 三、数据库设计

### 3.1 数据库概览

**数据库名称**：ai_platform  
**表数量**：5个核心表  
**总记录数**：141条  

### 3.2 表结构设计

**用户表 (users)**：存储用户基本信息和认证数据
- 主要字段：id, username, email, password_hash, avatar_url, is_active
- 记录数：11条
- 特性：唯一索引、软删除

**问题表 (questions)**：存储问题内容和智能筛选标签
- 主要字段：id, title, content, user_id, grade_level, subject, filter_tags
- 记录数：82条
- 特性：JSON字段存储筛选标签、外键关联

**视频表 (videos)**：存储视频元数据和播放统计
- 主要字段：id, title, file_path, file_size, format, user_id, view_count
- 记录数：43条
- 特性：文件系统+数据库混合存储

**反馈表 (feedbacks)**：存储用户反馈和处理状态
- 主要字段：id, title, content, type, status, priority, user_id
- 记录数：5条
- 特性：枚举类型、状态流转

**回答表 (answers)**：存储问题回答（待开发）
- 主要字段：id, content, question_id, user_id, is_ai_generated
- 记录数：0条
- 特性：支持AI生成标识

### 3.3 数据关系

```
users 1:N questions
users 1:N videos
users 1:N feedbacks
users 1:N answers
questions 1:N answers
```

---

## 四、核心功能模块

### 4.1 用户认证系统

**技术实现**：
- JWT Token认证机制
- Werkzeug密码哈希
- 会话状态管理
- 头像上传支持

**功能特性**：
- 用户注册登录
- 个人资料管理
- 权限控制
- 软删除保护

### 4.2 智能问答系统

**技术实现**：
- JSON字段存储筛选标签
- 多维度组合查询
- 实时数据同步
- 响应式UI设计

**筛选标签结构**：
```json
{
    "grade": "primary",
    "gradeLabel": "小学",
    "subject": "math",
    "subjectLabel": "数学",
    "timestamp": "2025-01-20T10:30:00"
}
```

**功能特性**：
- 年级层次筛选（小学/初中/高中/大学）
- 学科分类筛选（数学/英语/科学等）
- 问题自动保存
- 历史记录查询

### 4.3 视频学习系统

**技术实现**：
- HTML5原生播放器
- 混合存储架构
- 文件类型检测
- 元数据管理

**功能特性**：
- 多格式支持（MP4/WebM/OGV/MOV）
- 在线播放
- 播放统计
- 视频管理

**存储数据**：
- 视频文件：1.8GB，41个文件
- 数据库记录：43条元数据

### 4.4 反馈管理系统

**技术实现**：
- 枚举类型约束
- 状态流转控制
- 优先级管理
- 处理时间跟踪

**反馈类型**：BUG、FEATURE、COMPLAINT、PRAISE、OTHER

**处理流程**：PENDING → PROCESSING → RESOLVED → CLOSED

---

## 五、数据库管理系统

### 5.1 系统特性

**独立部署**：
- 前端端口：5173
- 后端端口：8081
- 避免端口冲突

**现代化UI**：
- 蓝紫渐变色系
- 响应式布局
- 实时数据更新

### 5.2 功能模块

**数据仪表板**：
- 系统状态监控
- 数据统计展示
- 增长趋势分析

**用户管理**：
- 用户信息查看编辑
- 状态管理
- 批量操作

**内容管理**：
- 问题内容审核
- 视频资源管理
- 反馈处理

**系统监控**：
- 数据库连接监控
- 性能指标展示
- 日志查看

---

## 六、技术特色

### 6.1 智能筛选系统

使用MySQL的JSON字段存储复杂筛选标签，支持年级和学科的多维度组合筛选，前端提供直观的筛选界面，后端支持复杂查询。

### 6.2 软删除机制

所有核心表都实现软删除（is_deleted字段），数据标记删除而非物理删除，保障数据安全，支持数据恢复。

### 6.3 混合存储架构

结构化数据存储在MySQL，媒体文件存储在文件系统，数据库存储元数据，文件系统存储大文件，实现最佳性能。

### 6.4 双项目架构

主平台专注用户体验，管理系统专注运营管理，独立部署避免相互影响，支持不同更新节奏。

---

## 七、系统数据

### 7.1 当前数据统计

| 模块 | 数量 | 状态 |
|------|------|------|
| 用户 | 11个 | 活跃 |
| 问题 | 82个 | 包含筛选功能 |
| 视频 | 43个(DB) + 41个(文件) | 待同步 |
| 反馈 | 5个 | 正常 |
| 回答 | 0个 | 待开发 |

### 7.2 技术指标

- 前端代码：约2万行
- 后端代码：约1万行
- API接口：30+个
- 功能模块：20+个
- 视频存储：1.8GB

### 7.3 功能完成度

**已完成功能（95%）**：
- 用户认证系统
- 智能问答筛选
- 视频上传播放
- 反馈管理
- 数据库管理后台

**待开发功能（5%）**：
- AI回答生成
- 视频数据同步
- 高级搜索

---

## 八、部署与运行

### 8.1 环境要求

- Python 3.8+
- Node.js 16+
- MySQL 8.0+
- 系统依赖：FFmpeg, libmagic

### 8.2 快速启动

**主平台**：
```bash
# 后端启动 (端口3003)
source venv/bin/activate
python run.py

# 前端启动 (端口3000)
cd react
npm start
```

**数据库管理系统**：
```bash
# 进入管理系统目录
cd db-admin-web

# 一键启动 (端口5173/8081)
./start.sh
```

### 8.3 数据库配置

- 主机：localhost:3306
- 用户：root
- 密码：123456
- 数据库：ai_platform

---

## 九、技术创新点

### 9.1 前后端分离架构

React+Flask完全分离，前端专注用户体验，后端专注业务逻辑，支持独立开发测试部署。

### 9.2 JSON字段应用

在关系型数据库中使用JSON字段存储复杂筛选标签，兼顾了结构化查询和灵活性。

### 9.3 软删除全覆盖

所有业务表统一实现软删除机制，提供数据安全保障和审计能力。

### 9.4 混合存储策略

数据库存储结构化数据，文件系统存储媒体文件，实现了性能和成本的平衡。

---

## 十、项目总结

智映教匠AI平台成功实现了现代化的教育技术平台架构，采用React+Flask+MySQL技术栈，构建了完整的前后端分离系统。项目包含用户管理、智能问答、视频学习、反馈管理等核心功能模块，并配备独立的数据库管理系统。

**技术成就**：
- 完整的全栈开发实践
- 现代化架构设计
- 数据库优化和安全机制
- 响应式用户界面

**功能完成度**：95%的核心功能已实现，系统稳定运行，数据安全可靠。

---

**文档编制日期**：2025年1月20日  
**技术团队**：广州大学网络空间安全学院  

---

*本技术说明书重点介绍了智映教匠AI平台的技术架构、功能实现和系统设计，为技术人员提供全面的技术参考。* 