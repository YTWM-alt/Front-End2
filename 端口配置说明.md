# 智映教匠AI平台 - 端口配置说明

## 📋 端口分配总览

### 🎯 主应用 (智映教匠AI平台)
- **后端服务**: 端口 `3003` 
  - 文件: `run.py`
  - 配置: `app.run(host='0.0.0.0', port=3003, debug=True)`
  - 访问地址: http://localhost:3003

- **前端服务**: 端口 `3000` (React默认)
  - 目录: `react/`
  - 工具: Create React App (react-scripts)
  - 代理配置: `"proxy": "http://localhost:3003"` (在package.json中)
  - 访问地址: http://localhost:3000

### 🔧 数据库管理系统 (db-admin-web)
- **后端服务**: 端口 `8081`
  - 文件: `db-admin-web/backend/run.py`
  - 配置: `app.run(host='0.0.0.0', port=8081, debug=True)`
  - 访问地址: http://localhost:8081

- **前端服务**: 端口 `5173`
  - 目录: `db-admin-web/frontend/`
  - 工具: Vite
  - 配置: `vite.config.js` 中 `port: 5173`
  - 代理配置: `target: 'http://localhost:8081'`
  - 访问地址: http://localhost:5173

## 🚀 服务启动命令

### 主应用
```bash
# 启动后端 (端口 3003)
python run.py

# 启动前端 (端口 3000)
cd react
npm start
```

### 数据库管理系统
```bash
# 一键启动 (推荐)
cd db-admin-web
./start.sh

# 或手动启动
# 后端 (端口 8081)
cd db-admin-web/backend
python run.py

# 前端 (端口 5173) 
cd db-admin-web/frontend
npm run dev
```

## 🔗 服务间通信

### API调用关系
- 主应用前端 → 主应用后端: `http://localhost:3003/api`
- 管理系统前端 → 管理系统后端: `http://localhost:8081/api`
- 管理系统后端 → MySQL数据库: `localhost:3306`

### 跨域配置
- 主应用: package.json中配置proxy
- 管理系统: Vite配置代理 + Flask CORS设置

## ⚠️ 重要注意事项

### 1. 端口冲突检查
在启动服务前，确保以下端口未被占用：
- `3000` - 主应用前端
- `3003` - 主应用后端  
- `5173` - 管理系统前端
- `8081` - 管理系统后端
- `3306` - MySQL数据库

### 2. 防火墙配置
如需外部访问，请开放相应端口：
```bash
# Ubuntu/Debian
sudo ufw allow 3000
sudo ufw allow 3003
sudo ufw allow 5173  
sudo ufw allow 8081
```

### 3. 生产环境
生产环境建议使用：
- Nginx反向代理
- 标准端口 (80/443)
- SSL证书配置
- 环境变量管理端口配置

## 📚 相关文件

### 端口配置文件
- `run.py` - 主应用后端端口
- `react/package.json` - 主应用前端代理配置
- `db-admin-web/backend/run.py` - 管理系统后端端口
- `db-admin-web/frontend/vite.config.js` - 管理系统前端端口和代理
- `db-admin-web/frontend/src/utils/api.js` - API基础URL配置

### 文档文件
- `db-admin-web/README.md` - 管理系统说明文档
- `db-admin-web/start.sh` - 一键启动脚本
- `db-admin-web/docs/数据库管理系统技术文档.md` - 技术文档

## 🔄 更新历史

- **2025-01-20**: 统一端口配置，解决8080/8081不一致问题
- **2025-01-20**: 创建本端口配置说明文档

---

**维护说明**: 如需修改端口配置，请同步更新所有相关文件和文档，确保一致性。 