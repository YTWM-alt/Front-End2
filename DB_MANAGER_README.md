# 数据库可视化管理工具

## 📋 功能介绍

这是一个为"未决定AI平台"量身定制的数据库可视化管理工具，提供美观的命令行界面来管理MySQL数据库中的数据。

### 🎯 主要功能

- **📊 数据库概览** - 查看所有数据表的统计信息和最近7天的活动情况
- **👥 用户管理** - 查看用户列表，包括注册时间、最后登录时间等
- **🎬 视频管理** - 查看视频列表，包括大小、时长、状态等信息
- **❓ 问题管理** - 查看问题列表，包括状态、优先级、浏览次数等
- **💬 反馈管理** - 查看用户反馈，按类型、优先级、状态分类显示
- **📤 数据导出** - 将数据库数据导出为JSON格式，支持单表或全量导出
- **🗑️ 数据清理** - 清理软删除的数据，释放存储空间

### 📈 统计信息

工具会显示以下统计信息：
- 各类数据的总数和已删除数量
- 最近7天的新增数据统计
- 视频存储空间使用情况
- 数据库健康状况

## 🚀 快速开始

### 1. 安装依赖

运行安装脚本自动安装所需依赖：

```bash
python install_db_manager.py
```

或者手动安装：

```bash
pip install rich
```

### 2. 运行工具

确保Flask应用正在运行，然后：

```bash
python db_manager.py
```

### 3. 使用说明

启动后，你会看到一个美观的菜单界面：

```
╔══════════════════════════════════════════════════════════════╗
║                    未决定AI平台                              ║
║                  数据库可视化管理工具                        ║
║                                                              ║
║  功能: 数据查看 • 用户管理 • 数据导出 • 系统维护            ║
╚══════════════════════════════════════════════════════════════╝

┌─────────────────── 🔧 主菜单 ───────────────────┐
│ 1      📊 数据库概览                            │
│ 2      👥 用户管理                              │
│ 3      🎬 视频管理                              │
│ 4      ❓ 问题管理                              │
│ 5      💬 反馈管理                              │
│ 6      📤 数据导出                              │
│ 7      🗑️ 数据清理                              │
│ 0      🚪 退出程序                              │
└─────────────────────────────────────────────────┘
```

## 📊 功能详解

### 数据库概览 (选项 1)

显示数据库的整体状况：
- 各表记录数统计
- 软删除记录统计  
- 最近7天新增记录统计
- 视频文件存储情况

### 用户管理 (选项 2)

查看用户列表，包含：
- 用户ID、用户名、邮箱
- 注册时间、最后登录时间
- 账户状态（活跃/禁用）

### 视频管理 (选项 3)

查看视频列表，包含：
- 视频ID、标题、格式
- 文件大小、时长
- 上传时间、处理状态

### 问题管理 (选项 4)

查看问题列表，包含：
- 问题ID、标题、状态
- 优先级、浏览次数
- 创建时间

### 反馈管理 (选项 5)

查看用户反馈，包含：
- 反馈ID、标题、类型（Bug/功能/投诉/表扬/其他）
- 优先级（低/中/高/紧急）
- 状态（待处理/处理中/已解决/已关闭）
- 创建时间

### 数据导出 (选项 6)

支持以下导出选项：
- `users` - 仅导出用户数据
- `videos` - 仅导出视频数据  
- `questions` - 仅导出问题数据
- `feedbacks` - 仅导出反馈数据
- `all` - 导出所有数据

导出文件格式：`{table}_export_{timestamp}.json`

### 数据清理 (选项 7)

清理软删除的数据：
- 显示待清理数据统计
- 确认后永久删除（不可恢复）
- 释放数据库存储空间

## ⚙️ 配置要求

### 环境要求
- Python 3.7+
- 已配置的MySQL数据库
- Flask应用已正确配置数据库连接

### 依赖包
- `rich` - 用于美观的命令行界面
- `sqlalchemy` - 数据库ORM
- `pymysql` - MySQL连接器

### 数据库连接

工具会自动使用Flask应用的数据库配置，确保以下文件存在且配置正确：
- `app/config/database.py` - 数据库连接配置
- `app/models/` - 数据模型定义
- `app/app_config.py` - 应用配置

## 🛠️ 故障排除

### 常见问题

1. **导入模块失败**
   ```
   ImportError: No module named 'app.config.database'
   ```
   解决：确保在项目根目录运行脚本

2. **数据库连接失败**
   ```
   数据库连接失败: (2003, "Can't connect to MySQL server")
   ```
   解决：检查MySQL服务是否启动，数据库配置是否正确

3. **权限错误**
   ```
   Access denied for user
   ```
   解决：检查数据库用户权限配置

4. **Rich库未安装**
   ```
   ModuleNotFoundError: No module named 'rich'
   ```
   解决：运行 `pip install rich` 或使用安装脚本

### 调试模式

如果遇到问题，可以查看Flask应用的日志输出，工具会显示详细的错误信息。

## 📝 更新日志

### v1.0.0 (2024-12-09)
- ✨ 首次发布
- 📊 数据库概览功能
- 👥 用户管理功能  
- 🎬 视频管理功能
- ❓ 问题管理功能
- 💬 反馈管理功能
- 📤 数据导出功能
- 🗑️ 数据清理功能
- 🎨 美观的命令行界面

## 📧 技术支持

如遇到问题或需要功能建议，请通过以下方式联系：
- 创建Issue
- 发送邮件给开发团队

## 📄 许可证

本工具是"未决定AI平台"的一部分，仅供内部使用。 