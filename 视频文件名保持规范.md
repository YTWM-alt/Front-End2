# 视频文件名保持规范说明文档

## 修改目标
确保AI生成的视频转移到video文件夹后，**必须以视频原本的名字存进数据库，不可以在中途变更名字**。

## 已修改的文件和位置

### 1. app/routes/video.py

#### AI视频处理函数 (process_ai_video)
- **位置**: 第283行
- **原始代码**: `title=f"AI产品-{file_stem}"`
- **修改后**: `title=file_stem`  # 使用原始文件名，不添加前缀
- **说明**: 移除了"AI产品-"前缀，确保使用原始视频文件名作为标题

#### 视频上传函数 (upload_video)
- **位置**: 第350-365行
- **原始代码**: 
  ```python
  timestamp = datetime.now().strftime('%Y-%m-%d_%H-%M-%S')
  new_filename = f"{timestamp}{file_extension}"
  ```
- **修改后**: 
  ```python
  safe_filename = secure_filename(file.filename)
  # 添加文件存在检查，避免覆盖
  ```
- **说明**: 移除时间戳前缀，保持原始文件名，只进行安全处理

### 2. db-admin-web/backend/app/routes.py

#### 数据库管理后端视频上传函数 (create_video)
- **位置**: 第707行
- **原始代码**: `unique_filename = f"{uuid.uuid4()}{file_ext}"`
- **修改后**: `safe_filename = secure_filename(file.filename)`
- **说明**: 移除UUID重命名，保持原始文件名，只进行安全处理

## 保持不变的正确实现

### 1. app/scripts/sync_videos.py
- **位置**: 第89行
- **代码**: `title=video_file.stem`
- **状态**: ✅ 已经正确使用原始文件名

### 2. import_videos.py
- **位置**: 第119行
- **代码**: `title=video_file.stem`
- **状态**: ✅ 已经正确使用原始文件名

### 3. app/scripts/migrate_data.py
- **位置**: 第264行
- **代码**: `title=file_path.stem`
- **状态**: ✅ 已经正确使用原始文件名

## 实现效果

### 修改前问题：
1. AI视频标题被加上"AI产品-"前缀
2. 上传的视频文件名被添加时间戳或UUID
3. 实际文件名与数据库中的标题不一致

### 修改后效果：
1. ✅ AI视频保持原始文件名作为标题
2. ✅ 上传视频保持原始文件名
3. ✅ 文件名与数据库标题完全一致
4. ✅ 添加了文件重复检查，避免意外覆盖

## 文件重复处理机制

为了避免文件覆盖，在保持原始文件名的同时，我们添加了以下检查：

1. **上传时检查**: 如果同名文件已存在，返回409错误码和明确的错误信息
2. **AI处理时检查**: 如果目标位置已有同名文件，跳过处理并删除源文件
3. **用户友好提示**: 明确告知用户文件已存在，需要重命名后再上传

## 注意事项

1. **文件安全**: 所有文件名都经过`secure_filename()`处理，确保安全性
2. **扩展名保持**: 文件扩展名完全保持原样
3. **标题一致**: 数据库中的title字段与实际文件名（不含扩展名）完全一致
4. **兼容性**: 修改向下兼容，不影响现有视频记录

## 验证方法

1. 上传一个名为"test_video.mp4"的视频
2. 检查数据库中title字段是否为"test_video"
3. 检查文件系统中是否保存为"test_video.mp4"
4. 验证AI生成的视频是否保持原始文件名

---

**重要提醒**: 此修改确保了视频文件名的一致性和可追溯性，符合用户的明确要求。 