# 项目与数据库说明文档

## 1. 项目概述

未决定AI平台是一个基于Flask和React的全栈应用，提供智能问答、视频管理、用户反馈等功能。系统包含两个主要部分：主项目平台和数据库管理系统。

### 1.1 系统特点

- **全栈应用架构**：前后端分离设计，前端React，后端Flask
- **用户认证系统**：支持注册、登录和个人信息管理
- **多媒体处理**：视频上传、处理和管理功能
- **AI智能问答**：提供问题保存和AI回答功能
- **反馈系统**：用户反馈收集和处理
- **数据库管理工具**：专用的数据库可视化和管理界面

## 2. 系统架构

### 2.1 技术栈

**主项目平台**
- **前端**：React 18、Material-UI、React Router、React Query、Zustand、Axios
- **后端**：Python 3.8+、Flask 3.0、SQLite/MySQL
- **数据存储**：基于文件系统和MySQL数据库

**数据库管理系统**
- **后端**：Python Flask框架构建RESTful API
- **前端**：原生JavaScript、Bootstrap 5和Chart.js
- **数据库连接**：mysql-connector-python库实现连接池管理

### 2.2 系统组件关系

```
未决定AI平台
├── 主项目平台
│   ├── 前端 (React)
│   │   ├── 用户界面组件
│   │   ├── 状态管理
│   │   └── API请求服务
│   └── 后端 (Flask)
│       ├── API路由
│       ├── 业务逻辑
│       └── 数据访问层
└── 数据库管理系统
    ├── Web界面
    ├── 数据库连接管理
    └── 查询执行与结果展示
```

## 3. 主项目平台

### 3.1 前端部分

**目录结构**
```
react/
├── src/               # 源代码
│   ├── components/    # React组件
│   │   ├── utils/         # 工具函数
│   │   └── App.js         # 主应用组件
├── public/            # 静态资源
└── package.json       # 前端依赖配置
```

**主要功能模块**
- 用户认证（登录/注册）
- 视频管理界面
- 问题提交与管理
- 个人中心
- 反馈系统

### 3.2 后端部分

**目录结构**
```
app/
├── __init__.py        # 应用初始化
├── config.py          # 配置文件
├── routes/            # 路由模块
│   ├── auth.py        # 认证相关
│   ├── video.py       # 视频管理
│   ├── question.py    # 问题管理
│   ├── feedback.py    # 反馈管理
│   └── ai.py          # AI服务
└── utils/             # 工具模块
    ├── error_handlers.py  # 错误处理
    └── file_handler.py    # 文件处理
```

**API端点**
- `/api/register`, `/api/login` - 用户认证
- `/api/videos` - 视频管理
- `/api/questions` - 问题管理
- `/api/feedback` - 反馈管理
- `/api/ai-status` - AI服务状态

## 4. 数据库管理系统

### 4.1 系统概述

数据库管理系统是一个基于Web的MySQL数据库管理工具，专为AI平台项目提供简洁直观的数据库操作界面。该系统支持数据库表的浏览、查询、结构分析和数据可视化。

### 4.2 核心文件结构

```
.
├── db_admin_run.py          # 服务启动入口
├── db_admin.py              # 数据库管理核心模块
├── start_db_admin.sh        # Linux启动脚本
├── start_db_admin.bat       # Windows启动脚本
├── database/                # 数据库配置和数据目录
│   └── db_config.json       # 数据库连接配置
├── logs/                    # 日志文件目录
│   └── db_admin.log         # 系统日志
├── static/                  # 静态资源目录
│   └── db_admin/            # 数据库管理系统静态资源
│       ├── scripts.js       # 前端JavaScript脚本
│       ├── chart-utils.js   # 图表工具脚本
│       └── styles.css       # 样式表
└── templates/               # 模板目录
    └── db_admin/            # 数据库管理系统模板
        ├── index.html       # 主页模板
        └── analysis.html    # 数据分析页面模板
```

### 4.3 主要功能

- **数据库连接管理**：配置连接参数、连接状态显示、自动重连
- **数据仪表盘**：数据统计、分布图表、增长趋势、交互式图表
- **表格数据管理**：表列表浏览、表数据查看、表结构查看、数据导出
- **自定义SQL查询**：SQL编辑器、查询结果展示、结果导出、错误处理
- **字段翻译功能**：自动将英文字段名翻译为中文显示

## 5. 数据库结构

系统连接的是名为`ai_platform`的MySQL数据库，包含以下主要表：

### 5.1 用户表 (users)

| 字段名          | 类型           | 说明                 |
|---------------|----------------|---------------------|
| id            | int            | 主键，自增            |
| username      | varchar(50)    | 用户名，唯一           |
| email         | varchar(100)   | 邮箱，唯一            |
| password_hash | varchar(128)   | 密码哈希值            |
| avatar_url    | varchar(200)   | 头像URL              |
| is_active     | tinyint(1)     | 是否激活              |
| created_at    | datetime       | 创建时间              |
| updated_at    | datetime       | 更新时间              |
| is_deleted    | tinyint(1)     | 是否删除              |

### 5.2 视频表 (videos)

| 字段名          | 类型           | 说明                 |
|---------------|----------------|---------------------|
| id            | int            | 主键，自增            |
| title         | varchar(200)   | 视频标题              |
| description   | text           | 视频描述              |
| file_path     | varchar(500)   | 文件路径              |
| user_id       | int            | 上传用户ID，外键       |
| view_count    | int            | 观看次数              |
| created_at    | datetime       | 创建时间              |
| is_deleted    | tinyint(1)     | 是否删除              |

### 5.3 问题表 (questions)

| 字段名         | 类型           | 说明                 |
|--------------|----------------|---------------------|
| id           | int            | 主键，自增            |
| title        | varchar(200)   | 问题标题              |
| content      | text           | 问题内容              |
| user_id      | int            | 提问用户ID，外键       |
| status       | varchar(20)    | 问题状态              |
| priority     | int            | 优先级                |
| view_count   | int            | 查看次数              |
| answer_count | int            | 回答数量              |
| created_at   | datetime       | 创建时间              |
| updated_at   | datetime       | 更新时间              |
| is_deleted   | tinyint(1)     | 是否删除              |

### 5.4 回答表 (answers)

| 字段名          | 类型         | 说明                 |
|---------------|--------------|---------------------|
| id            | int          | 主键，自增            |
| content       | text         | 回答内容              |
| question_id   | int          | 问题ID，外键          |
| user_id       | int          | 回答用户ID，外键       |
| is_accepted   | tinyint(1)   | 是否被采纳            |
| is_ai_generated| tinyint(1)  | 是否AI生成            |
| like_count    | int          | 点赞数                |
| created_at    | datetime     | 创建时间              |
| is_deleted    | tinyint(1)   | 是否删除              |

### 5.5 反馈表 (feedbacks)

| 字段名        | 类型                                              | 说明                |
|-------------|---------------------------------------------------|-------------------|
| id          | int                                               | 主键，自增           |
| title       | varchar(200)                                      | 反馈标题             |
| content     | text                                              | 反馈内容             |
| user_id     | int                                               | 用户ID，外键         |
| type        | enum('BUG','FEATURE','COMPLAINT','PRAISE','OTHER')| 反馈类型             |
| status      | enum('PENDING','PROCESSING','RESOLVED','CLOSED')  | 处理状态             |
| priority    | enum('LOW','MEDIUM','HIGH','URGENT')              | 优先级              |
| created_at  | datetime                                          | 创建时间             |
| is_deleted  | tinyint(1)                                        | 是否删除             |

## 6. 部署与使用指南

### 6.1 环境要求

- Python 3.8+
- Node.js 16+
- MySQL 5.7+
- 系统依赖：libmagic1, ffmpeg (Linux/Mac)
- Windows: ffmpeg, Python-magic-bin

### 6.2 项目部署

#### 后端部署

```bash
# 创建并激活虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
.\venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt

# 初始化数据库
python init_db.py

# 启动后端服务
./start.sh  # Linux/Mac
start.bat   # Windows
```

#### 前端部署

```bash
# 进入前端目录
cd react

# 安装依赖
npm install

# 启动开发服务器
npm start
```

### 6.3 数据库管理系统部署

```bash
# 确保在项目根目录
cd /path/to/project

# 启动数据库管理系统
./start_db_admin.sh  # Linux
start_db_admin.bat   # Windows

# 系统将在 http://localhost:3003/ 运行
```

## 7. 常见问题解答

### 7.1 连接问题

**Q: 无法连接到数据库**  
A: 请检查MySQL服务是否正在运行，以及用户名、密码和数据库名是否正确。

**Q: 页面显示"Address already in use"错误**  
A: 系统已经在运行，请使用`pkill -f "python.*db_admin_run.py"`终止现有进程后重试。

### 7.2 使用问题

**Q: 表格列宽不对齐**  
A: 系统使用固定行高和自适应列宽设计，确保CSS样式正确加载。刷新页面或清除浏览器缓存可能解决此问题。

**Q: 字段名显示为英文**  
A: 如果某些字段没有被翻译，可以在`scripts.js`中的`fieldTranslations`对象中添加映射。

**Q: 执行大型查询时界面卡顿**  
A: 系统使用异步查询执行，但过大的结果集仍可能导致性能问题。建议限制查询返回的数据量，或使用更精确的筛选条件。

## 8. 总结

未决定AI平台是一个功能完整的全栈应用，包含主项目平台和数据库管理系统两个主要部分。主项目平台采用React和Flask构建，提供用户认证、视频管理、问题管理、反馈系统等功能。数据库管理系统则为平台提供了直观的数据库操作界面，支持数据浏览、查询、结构分析和可视化。

两个系统协同工作，为用户提供完整的AI平台体验，同时方便开发人员和非技术人员轻松管理和分析数据库内容。 